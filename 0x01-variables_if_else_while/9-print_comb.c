#include <stdio.h>

/**
 * main - Entry point.
 *
 * Return: Always 0(Success).
 */
int main()
    for (int i = 0; i < 10; i++) 
        putchar(i + '0');r
        if (i < 9) {
            putchar(',');
            putchar(' ');
        }
    }

    return 0;
}
